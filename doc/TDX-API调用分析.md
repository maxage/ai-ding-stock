# TDX-API 调用分析文档

> **文档说明**: 本文档分析 ai-ding-stock 项目如何调用 TDX-API 项目，以及具体使用了哪些接口和参数。  
> **生成时间**: 2025-01-27  
> **TDX-API项目**: https://github.com/oficcejo/tdx-api  
> **TDX-API文档**: https://github.com/oficcejo/tdx-api/blob/main/API_接口文档.md

---

## 📋 目录

- [项目关系概述](#项目关系概述)
- [TDX-API 接口清单](#tdx-api-接口清单)
- [ai-ding-stock 调用分析](#ai-ding-stock-调用分析)
- [接口调用详情](#接口调用详情)
- [数据流程](#数据流程)
- [参数使用统计](#参数使用统计)
- [未使用的接口](#未使用的接口)
- [数据转换说明](#数据转换说明)
- [调用示例](#调用示例)
- [总结](#总结)

---

## 项目关系概述

### TDX-API 项目

**项目地址**: https://github.com/oficcejo/tdx-api  
**项目定位**: 股票数据API服务，提供A股实时行情、K线、分时等数据接口  
**服务端口**: 默认 8080（可配置）  
**技术栈**: 基于通达信数据源封装为HTTP RESTful API

### ai-ding-stock 项目

**项目定位**: AI股票分析系统，调用TDX-API获取数据，使用大模型进行分析  
**TDX客户端**: `stock/tdx_client.go`  
**配置项**: `config_stock.json` 中的 `tdx_api_url`

### 调用关系

```
┌─────────────────────────┐
│   ai-ding-stock         │
│  (股票分析系统)          │
│                         │
│  stock/tdx_client.go    │
└──────────┬──────────────┘
           │ HTTP请求
           ▼
┌─────────────────────────┐
│   TDX-API 服务          │
│  (股票数据API)           │
│  http://host:8080       │
└─────────────────────────┘
           │
           ▼
┌─────────────────────────┐
│   通达信数据源          │
└─────────────────────────┘
```

---

## TDX-API 接口清单

根据 [TDX-API接口文档](https://raw.githubusercontent.com/oficcejo/tdx-api/main/API_%E6%8E%A5%E5%8F%A3%E6%96%87%E6%A1%A3.md)，TDX-API 提供以下接口：

### 1. 基础接口

| 接口路径 | 方法 | 说明 | 状态 |
|---------|------|------|------|
| `/api/quote` | GET | 获取五档行情 | ✅ **已使用** |
| `/api/kline` | GET | 获取K线数据 | ✅ **已使用** |
| `/api/minute` | GET | 获取分时数据 | ✅ **已使用** |
| `/api/search` | GET | 搜索股票代码 | ⚠️ **已定义未使用** |
| `/api/trade` | GET | 获取分时成交明细 | ❌ **未使用** |

### 2. 扩展接口

| 接口路径 | 方法 | 说明 | 状态 |
|---------|------|------|------|
| `/api/kline-all/tdx` | GET | 通达信原始历史K线 | ❌ **未使用** |
| `/api/kline-all/ths` | GET | 同花顺前复权历史K线 | ❌ **未使用** |
| `/api/batch-quote` | POST | 批量获取行情 | ⚠️ **已定义未使用** |

### 3. 响应格式

所有接口统一返回格式：

```json
{
  "code": 0,           // 0=成功, -1=失败
  "message": "success", // 提示信息
  "data": {}           // 数据内容
}
```

---

## ai-ding-stock 调用分析

### 调用位置

**主调用文件**: `stock/analyzer.go` 的 `Analyze()` 方法

### 调用流程

```go
// 1. 获取实时行情
quote, err := a.TDXClient.GetQuote(a.AnalysisConfig.StockCode)

// 2. 获取日K线数据（最近60天）
dayKline, err := a.TDXClient.GetKline(a.AnalysisConfig.StockCode, "day", 60)

// 3. 获取30分钟K线数据（最近100条）
min30Kline, err := a.TDXClient.GetKline(a.AnalysisConfig.StockCode, "minute30", 100)

// 4. 获取今日分时数据
minuteData, err := a.TDXClient.GetMinute(a.AnalysisConfig.StockCode, "")
```

### TDX客户端实现

**文件**: `stock/tdx_client.go`

**核心类**:
```go
type TDXClient struct {
    BaseURL    string
    HTTPClient *http.Client  // 超时时间: 10秒
}
```

---

## 接口调用详情

### 1. 获取五档行情 (`GetQuote`)

#### TDX-API 接口

**路径**: `GET /api/quote`  
**参数**:
- `code` (string, 必填): 股票代码，支持多个逗号分隔

**请求示例**:
```
GET /api/quote?code=000001
```

**响应数据结构**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "Exchange": 0,
      "Code": "000001",
      "K": {
        "Last": 12250,    // 昨收价（厘）
        "Open": 12300,    // 开盘价（厘）
        "High": 12600,    // 最高价（厘）
        "Low": 12280,     // 最低价（厘）
        "Close": 12500    // 收盘价/最新价（厘）
      },
      "TotalHand": 1235000,    // 总手数
      "Intuition": 100,        // 现量
      "Amount": 156000000,     // 成交额（厘）
      "InsideDish": 520000,    // 内盘
      "OuterDisc": 715000,     // 外盘
      "BuyLevel": [...],       // 买五档
      "SellLevel": [...]       // 卖五档
    }
  ]
}
```

#### ai-ding-stock 调用

**方法**: `TDXClient.GetQuote(code string)`

**代码位置**: `stock/tdx_client.go:110`

**调用方式**:
```go
func (c *TDXClient) GetQuote(code string) (*QuoteData, error) {
    url := fmt.Sprintf("%s/api/quote?code=%s", c.BaseURL, code)
    // ... HTTP请求和解析
    return &quotes[0], nil  // 返回第一个结果
}
```

**实际调用**:
```go
// stock/analyzer.go:69
quote, err := a.TDXClient.GetQuote(a.AnalysisConfig.StockCode)
```

**使用的参数**:
- ✅ `K.Open` - 开盘价（用于计算涨跌幅）
- ✅ `K.High` - 最高价
- ✅ `K.Low` - 最低价
- ✅ `K.Close` - 最新价/收盘价（当前价格）
- ✅ `K.Last` - 昨收价（用于计算涨跌幅）
- ✅ `TotalHand` - 总手数（转换为股数）
- ✅ `Amount` - 成交额（厘转元）
- ✅ `InsideDish` - 内盘（用于计算内外盘比）
- ✅ `OuterDisc` - 外盘（用于计算内外盘比）
- ✅ `BuyLevel` - 买五档（用于分析盘口力量）
- ✅ `SellLevel` - 卖五档（用于分析盘口力量）

**未使用的参数**:
- ❌ `Exchange` - 交易所代码
- ❌ `Active1` / `Active2` - 活跃度指标
- ❌ `ServerTime` - 服务器时间
- ❌ `Intuition` - 现量
- ❌ `Rate` - 涨跌率

---

### 2. 获取K线数据 (`GetKline`)

#### TDX-API 接口

**路径**: `GET /api/kline`  
**参数**:
- `code` (string, 必填): 股票代码
- `type` (string, 可选): K线类型，默认 `day`
- `adjust` (int, 可选): 复权类型（0=不复权, 1=前复权, 2=后复权）

**注意**: 根据TDX-API文档，日/周/月K线默认行为是优先返回同花顺前复权数据，若第三方源不可用则自动回退到通达信原始数据。但ai-ding-stock项目显式指定`adjust=0`以获取不复权数据，确保与实时行情价格一致。

**K线类型**:
- `minute1` - 1分钟K线
- `minute5` - 5分钟K线
- `minute15` - 15分钟K线
- `minute30` - 30分钟K线 ✅ **已使用**
- `hour` - 60分钟/小时K线
- `day` - 日K线 ✅ **已使用**
- `week` - 周K线
- `month` - 月K线

**请求示例**:
```
GET /api/kline?code=000001&type=day&adjust=0
GET /api/kline?code=000001&type=minute30&adjust=0
```

**响应数据结构**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "Count": 100,
    "List": [
      {
        "Last": 12250,      // 昨收价（厘）
        "Open": 12300,      // 开盘价（厘）
        "High": 12600,      // 最高价（厘）
        "Low": 12280,       // 最低价（厘）
        "Close": 12500,     // 收盘价（厘）
        "Volume": 1235000,  // 成交量（手）
        "Amount": 156000000,// 成交额（厘）
        "Time": "2024-11-03T00:00:00Z",
        "UpCount": 0,
        "DownCount": 0
      }
    ]
  }
}
```

#### ai-ding-stock 调用

**方法**: `TDXClient.GetKline(code string, klineType string, limit int)`

**代码位置**: `stock/tdx_client.go:147`

**调用方式**:
```go
func (c *TDXClient) GetKline(code string, klineType string, limit int) (*KlineData, error) {
    url := fmt.Sprintf("%s/api/kline?code=%s&type=%s&adjust=0", c.BaseURL, code, klineType)
    // ... HTTP请求和解析
    // 限制返回数量（取最近的limit条）
    if limit > 0 && len(klineData.List) > limit {
        klineData.List = klineData.List[len(klineData.List)-limit:]
    }
    return &klineData, nil
}
```

**实际调用**:
```go
// stock/analyzer.go:75 - 获取日K线（最近60天）
dayKline, err := a.TDXClient.GetKline(a.AnalysisConfig.StockCode, "day", 60)

// stock/analyzer.go:81 - 获取30分钟K线（最近100条）
min30Kline, err := a.TDXClient.GetKline(a.AnalysisConfig.StockCode, "minute30", 100)
```

**参数说明**:
- `adjust=0`: **不复权**数据（与实时行情价格一致）
- `limit` 参数: 客户端侧限制返回条数

**使用的参数** (从K线数据中):

**日K线使用** (`dayKline`):
- ✅ `List[].Close` - 收盘价（计算MA5/10/20/60、RSI、波动率）
- ✅ `List[].Volume` - 成交量（展示在AI提示词中近5日趋势）
- ✅ `List[].Time` - 时间（展示近5日趋势）
- ✅ `len(List)` - 数据条数（用于显示"最近X个交易日数据"）

**30分钟K线使用** (`min30Kline`):
- ⚠️ **仅使用了数量信息**：只使用 `len(min30Kline.List)` 在AI提示词中显示"最近X条数据"
- ❌ **未使用实际K线数据**：未将30分钟K线的价格、成交量等数据传入AI分析

**未使用的参数**:
- ❌ `List[].Open` - 开盘价
- ❌ `List[].High` - 最高价
- ❌ `List[].Low` - 最低价
- ❌ `List[].Last` - 昨收价
- ❌ `List[].Amount` - 成交额
- ❌ `List[].UpCount` / `DownCount` - 涨跌数（仅指数有效）
- ❌ `Count` - 数据条数

---

### 3. 获取分时数据 (`GetMinute`)

#### TDX-API 接口

**路径**: `GET /api/minute`  
**参数**:
- `code` (string, 必填): 股票代码
- `date` (string, 可选): 日期（YYYYMMDD格式），默认当天

**注意**: 根据TDX-API文档最新版本，该接口严格按照请求日期返回结果，不再自动回退其他交易日；若指定日期无数据，将返回空列表。这与较早版本的"自动回退"行为不同。

**请求示例**:
```
GET /api/minute?code=000001
GET /api/minute?code=000001&date=20241103
```

**响应数据结构**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "Count": 240,
    "List": [
      {
        "Time": "09:31",
        "Price": 12300,    // 价格（厘）
        "Number": 1500     // 成交量（手）
      }
    ]
  }
}
```

**数据说明**:
- 交易时段：9:30-11:30（120分钟）, 13:00-15:00（120分钟）
- 共240个数据点
- 非交易时间可能返回空列表

#### ai-ding-stock 调用

**方法**: `TDXClient.GetMinute(code string, date string)`

**代码位置**: `stock/tdx_client.go:184`

**调用方式**:
```go
func (c *TDXClient) GetMinute(code string, date string) (*MinuteData, error) {
    urlStr := fmt.Sprintf("%s/api/minute?code=%s", c.BaseURL, code)
    if date != "" {
        urlStr += "&date=" + date
    }
    // ... HTTP请求和解析
}
```

**实际调用**:
```go
// stock/analyzer.go:87 - 获取今日分时数据（date为空，使用默认当天）
minuteData, err := a.TDXClient.GetMinute(a.AnalysisConfig.StockCode, "")
if err != nil {
    log.Printf("⚠️  获取分时数据失败（可能非交易时间）: %v", err)
    minuteData = nil  // 允许失败
}
```

**使用的参数**:
- ⚠️ **获取了但未使用** - 虽然获取了分时数据，但在 `buildAnalysisPrompt()` 方法中并未实际使用

**未使用的参数**:
- ❌ `Count` - 数据条数
- ❌ `List[].Time` - 时间点
- ❌ `List[].Price` - 价格
- ❌ `List[].Number` - 成交量

**错误处理**:
- 非交易时间获取失败是**允许的**，不会导致分析失败
- 失败时将 `minuteData` 设为 `nil`

---

## 数据流程

### 完整数据流

```
[定时任务触发]
    ↓
[检查交易时间]
    ↓
┌─────────────────────────────────────┐
│  调用 TDX-API (4个HTTP请求)         │
├─────────────────────────────────────┤
│  1. GET /api/quote?code=000001      │
│     → 获取实时五档行情               │
│                                     │
│  2. GET /api/kline?code=000001      │
│        &type=day&adjust=0           │
│     → 获取日K线（限制60天）          │
│                                     │
│  3. GET /api/kline?code=000001      │
│        &type=minute30&adjust=0      │
│     → 获取30分钟K线（限制100条）     │
│                                     │
│  4. GET /api/minute?code=000001     │
│     → 获取今日分时数据（可选）       │
└─────────────────────────────────────┘
    ↓
[数据解析和转换]
    ├─ 厘 → 元 (价格、成交额)
    ├─ 手 → 股 (成交量)
    └─ 计算技术指标
    ↓
[构建AI提示词]
    ├─ 实时行情数据
    ├─ 五档盘口
    ├─ 技术指标
    └─ 近期趋势
    ↓
[AI分析]
    ↓
[生成交易信号]
```

### 数据使用流向

```
TDX-API 返回数据
    ↓
stock/tdx_client.go (解析)
    ↓
stock/analyzer.go (使用)
    ├─ calculateTechnicalIndicators() (计算指标)
    └─ buildAnalysisPrompt() (构建提示词)
    ↓
AI模型分析
```

---

## 参数使用统计

### 实时行情参数使用情况

| 参数 | 字段路径 | 用途 | 状态 |
|-----|---------|------|------|
| 当前价格 | `quote.K.Close` | 主要价格指标 | ✅ **使用** |
| 开盘价 | `quote.K.Open` | 涨跌幅计算 | ✅ **使用** |
| 最高价 | `quote.K.High` | 技术分析 | ✅ **使用** |
| 最低价 | `quote.K.Low` | 技术分析 | ✅ **使用** |
| 昨收价 | `quote.K.Last` | 涨跌幅计算 | ✅ **使用** |
| 总手数 | `quote.TotalHand` | 成交量转换 | ✅ **使用** |
| 成交额 | `quote.Amount` | 量价分析 | ✅ **使用** |
| 内盘 | `quote.InsideDish` | 内外盘比计算 | ✅ **使用** |
| 外盘 | `quote.OuterDisc` | 内外盘比计算 | ✅ **使用** |
| 买五档 | `quote.BuyLevel[]` | 盘口力量分析 | ✅ **使用** |
| 卖五档 | `quote.SellLevel[]` | 盘口力量分析 | ✅ **使用** |
| 交易所 | `quote.Exchange` | - | ❌ **未使用** |
| 现量 | `quote.Intuition` | - | ❌ **未使用** |
| 服务器时间 | `quote.ServerTime` | - | ❌ **未使用** |
| 涨跌率 | `quote.Rate` | - | ❌ **未使用** |

**使用率**: 12/17 = 70.6%

### 日K线参数使用情况

| 参数 | 字段路径 | 用途 | 状态 |
|-----|---------|------|------|
| 收盘价 | `dayKline.List[].Close` | MA/RSI/波动率计算 | ✅ **使用** |
| 成交量 | `dayKline.List[].Volume` | AI提示词展示 | ✅ **使用** |
| 时间 | `dayKline.List[].Time` | 近5日趋势展示 | ✅ **使用** |
| 开盘价 | `dayKline.List[].Open` | - | ❌ **未使用** |
| 最高价 | `dayKline.List[].High` | - | ❌ **未使用** |
| 最低价 | `dayKline.List[].Low` | - | ❌ **未使用** |
| 昨收价 | `dayKline.List[].Last` | - | ❌ **未使用** |
| 成交额 | `dayKline.List[].Amount` | - | ❌ **未使用** |
| 涨跌数 | `dayKline.List[].UpCount` | - | ❌ **未使用** |

**使用率**: 3/9 = 33.3%

### 30分钟K线参数使用情况

| 状态 | 说明 |
|-----|------|
| ⚠️ **获取但未使用** | 虽然获取了30分钟K线数据，但在构建AI提示词时并未实际使用 |

### 分时数据参数使用情况

| 状态 | 说明 |
|-----|------|
| ⚠️ **获取但未使用** | 虽然获取了分时数据，但在构建AI提示词时并未实际使用 |

---

## 未使用的接口

### 1. 搜索股票 (`/api/search`)

**接口定义**: `TDXClient.SearchStock()` 已在 `stock/tdx_client.go:219` 定义  
**状态**: ❌ **未在代码中使用**

**潜在用途**:
- 股票代码搜索
- 配置界面中帮助用户查找股票

**建议**: 可以在Web配置界面中使用，方便用户搜索股票代码

---

### 2. 获取分时成交 (`/api/trade`)

**接口定义**: ❌ **未定义**  
**状态**: ❌ **未使用**

**潜在用途**:
- 分析大单流向
- 监控异常交易

**建议**: 如需要分析成交明细，可以添加此接口

---

### 3. 批量获取行情 (`/api/batch-quote`)

**接口定义**: `TDXClient.BatchGetQuote()` 已在 `stock/tdx_client.go:250` 定义  
**状态**: ⚠️ **已定义但未使用**

**潜在用途**:
- 优化多只股票的行情获取（减少HTTP请求次数）

**建议**: 如果监控多只股票，可以使用批量接口优化性能

---

### 4. 全量历史K线 (`/api/kline-all/tdx`, `/api/kline-all/ths`)

**接口定义**: ❌ **未在tdx_client.go中定义**  
**状态**: ❌ **未使用**

**说明**:
- `/api/kline-all/tdx` - 通达信原始历史K线（全量，按800条一批拼接）
- `/api/kline-all/ths` - 同花顺前复权历史K线（全量，一次性返回）

**注意**: 这些接口在TDX-API项目中已提供，但ai-ding-stock项目未实现客户端调用方法。

**当前方案**: 使用 `/api/kline?type=day&adjust=0` 并限制返回60天

**潜在用途**:
- 需要更长时间的历史数据（如计算长期均线）
- 需要前复权数据（用于更准确的技术分析，特别是除权除息后）
- 全量数据支持，不受800条限制

**建议**: 如需更长的历史数据或复权数据，可以在tdx_client.go中添加对应的客户端方法

---

## 数据转换说明

### 单位转换函数

**文件**: `stock/tdx_client.go:283-296`

```go
// 1. 价格转换：厘 → 元
func PriceToYuan(li int) float64 {
    return float64(li) / 1000.0
}
// 示例: 12500厘 → 12.50元

// 2. 成交量转换：手 → 股
func VolumeToShares(hands int64) int64 {
    return hands * 100
}
// 示例: 1235手 → 123500股

// 3. 成交额转换：厘 → 元
func AmountToYuan(amount float64) float64 {
    return amount / 1000.0
}
// 示例: 156000000厘 → 156000元
```

### 转换使用位置

**文件**: `stock/analyzer.go:128-143`

```go
// 价格转换
currentPrice := PriceToYuan(quote.K.Close)
data["current_price"] = currentPrice
data["open_price"] = PriceToYuan(quote.K.Open)
data["high_price"] = PriceToYuan(quote.K.High)
data["low_price"] = PriceToYuan(quote.K.Low)
data["prev_close"] = PriceToYuan(quote.K.Last)

// 成交量转换
data["volume"] = VolumeToShares(quote.TotalHand)

// 成交额转换
data["amount"] = AmountToYuan(quote.Amount)
```

---

## 调用示例

### 完整调用流程示例

以分析股票 `000001`（平安银行）为例：

```go
// 1. 创建TDX客户端
tdxClient := stock.NewTDXClient("http://192.168.1.222:8181")

// 2. 获取实时行情
quote, err := tdxClient.GetQuote("000001")
// HTTP请求: GET http://192.168.1.222:8181/api/quote?code=000001
// 返回: QuoteData 结构

// 3. 获取日K线（最近60天）
dayKline, err := tdxClient.GetKline("000001", "day", 60)
// HTTP请求: GET http://192.168.1.222:8181/api/kline?code=000001&type=day&adjust=0
// 返回: KlineData 结构（List包含60条日K线）

// 4. 获取30分钟K线（最近100条）
min30Kline, err := tdxClient.GetKline("000001", "minute30", 100)
// HTTP请求: GET http://192.168.1.222:8181/api/kline?code=000001&type=minute30&adjust=0
// 返回: KlineData 结构（List包含100条30分钟K线）

// 5. 获取今日分时数据
minuteData, err := tdxClient.GetMinute("000001", "")
// HTTP请求: GET http://192.168.1.222:8181/api/minute?code=000001
// 返回: MinuteData 结构（List包含240个分时点）
```

### HTTP请求示例

```bash
# 1. 获取五档行情
curl "http://192.168.1.222:8181/api/quote?code=000001"

# 2. 获取日K线
curl "http://192.168.1.222:8181/api/kline?code=000001&type=day&adjust=0"

# 3. 获取30分钟K线
curl "http://192.168.1.222:8181/api/kline?code=000001&type=minute30&adjust=0"

# 4. 获取分时数据
curl "http://192.168.1.222:8181/api/minute?code=000001"
```

---

## 总结

### 调用情况总览

| 接口 | 调用状态 | 使用率 | 说明 |
|-----|---------|--------|------|
| `/api/quote` | ✅ **已使用** | 70.6% | 获取实时行情，使用12/17个参数 |
| `/api/kline` (day) | ✅ **已使用** | 33.3% | 获取日K线，使用3/9个参数 |
| `/api/kline` (minute30) | ⚠️ **获取未用** | 0% | 获取了但未使用 |
| `/api/minute` | ⚠️ **获取未用** | 0% | 获取了但未使用 |
| `/api/search` | ❌ **未使用** | 0% | 已定义接口但未调用 |
| `/api/trade` | ❌ **未使用** | 0% | 未定义接口 |
| `/api/kline-all/*` | ❌ **未使用** | 0% | 未定义接口 |
| `/api/batch-quote` | ⚠️ **定义未用** | 0% | 已定义接口但未调用 |

### 关键发现

1. **核心接口使用**: 
   - ✅ 实时行情 (`/api/quote`) 是核心接口，使用最充分
   - ✅ 日K线 (`/api/kline?type=day`) 用于技术指标计算
   - ⚠️ 30分钟K线和分时数据获取了但未实际使用

2. **数据利用率**:
   - 实时行情: 70.6% 参数被使用
   - 日K线: 33.3% 参数被使用
   - 部分数据获取但未传递给AI分析

3. **优化空间**:
   - 可以考虑使用批量接口优化多股票场景
   - 30分钟K线和分时数据可以加入到AI提示词中
   - 可以使用搜索接口在配置界面中帮助用户

4. **参数选择**:
   - `adjust=0` 选择不复权数据（与实时行情一致）
   - 日K线限制60天（足够计算MA60）
   - 30分钟K线限制100条（约50小时数据）

5. **错误处理**:
   - 分时数据获取失败允许继续（非交易时间）
   - 其他接口失败会导致分析失败

### 建议

#### 1. 优化数据使用
- ✅ 将30分钟K线数据加入到AI提示词中（短期趋势）
- ✅ 将分时数据加入到AI提示词中（当日走势）

#### 2. 性能优化
- ✅ 多股票监控时使用批量接口 (`/api/batch-quote`)
- ✅ 考虑缓存不常变化的数据（如日K线）

#### 3. 功能扩展
- ✅ 在Web配置界面使用搜索接口 (`/api/search`)
- ✅ 如需更准确分析，可考虑使用前复权数据 (`/api/kline-all/ths`)

#### 4. 数据完整性
- ✅ 考虑使用K线的OHLC完整数据（而不只是Close）
- ✅ 可以添加成交明细分析（`/api/trade`）

---

**文档结束**

*本文档基于代码分析和TDX-API接口文档整理，详细说明了ai-ding-stock项目如何调用TDX-API以及使用的具体参数。*

