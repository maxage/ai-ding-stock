# ================================
# 股票分析系统 Docker 环境变量配置
# ================================

# 时区设置
TZ=Asia/Shanghai

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# API服务端口（宿主机端口）
API_PORT=8080

# Web服务端口（宿主机端口）
WEB_PORT=80

# API Token（用于前端重启后端等操作）
# 说明：
# - 此Token用于前端通过Web界面重启后端服务等敏感操作
# - 建议使用强密码（至少16位，包含字母、数字和特殊字符）
# - 可以通过环境变量设置，也可以在配置文件中设置（配置文件优先级更高）
# - 如果都不设置，系统将自动生成一个默认Token（仅用于测试，不建议生产环境使用）
# - 在生产环境中，强烈建议通过环境变量设置，避免Token泄露
# 示例：API_TOKEN=your-strong-random-token-here-min-16-chars
API_TOKEN=

# Docker网络子网
SUBNET=172.20.0.0/16

# 容器内存限制（可选）
# MEMORY_LIMIT=512m

# 容器CPU限制（可选）
# CPU_LIMIT=1.0

# 健康检查间隔（秒）
HEALTHCHECK_INTERVAL=30

# 日志文件配置
LOG_MAX_SIZE=10m
LOG_MAX_FILE=3

# 构建参数
GOLANG_VERSION=1.24
ALPINE_VERSION=3.19
NGINX_VERSION=1.25

